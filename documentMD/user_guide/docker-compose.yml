version: "3"
services:
  service_jekyll:
    image: "jekyll/jekyll:pages"
    volumes:
      - .:/srv/jekyll/
    command: >
      bash -c "gem install webrick &&
      gem install jekyll-polyglot -v 1.5.1 &&
      jekyll serve"
    ports:
      - "4000:4000"
    tty: true
    stdin_open: true
