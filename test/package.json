{
  "name": "test",
  "version": "1.0.0",
  "type": "module",
  "main": "cypress.config.js",
  "scripts": {
    "test": "npx cypress open",
    "test-all": "npx cypress run --browser chrome",
    "test:e2e": "npm run test:e2e:start && npm run test:e2e:run; npm run test:e2e:stop",
    "test:e2e:start": "docker compose up -d --wait",
    "test:e2e:run": "npm run test-all",
    "test:e2e:stop": "docker compose down",
    "test:ct:open": "cypress open --component",
    "test:ct:run": "cypress run --component --browser chrome",
    "start": "cd ../; npm start",
    "build": "cd ../; npm run build"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@cypress/skip-test": "^2.6.1",
    "@cypress/webpack-preprocessor": "^7.0.1",
    "chrome": "^0.1.0",
    "cypress-delete-downloads-folder": "^0.0.6",
    "cypress-real-events": "^1.15.0",
    "cypress-wait-until": "^3.0.2",
    "fs-extra": "^11.2.0",
    "simple-ssh": "^1.1.1",
    "tar": "^7.4.3"
  },
  "devDependencies": {
    "@cypress/vue": "^6.0.2",
    "@vitejs/plugin-vue": "^6.0.3",
    "cypress": "^15.9.0",
    "vite": "^7.3.1"
  }
}
