{
  "name": "test",
  "version": "1.0.0",
  "type": "module",
  "main": "cypress.config.js",
  "scripts": {
    "test": "npx cypress open",
    "gateway": "GW_PORT=3001 REAL_APP=http://localhost:8089 MOCK_SIO=http://localhost:3101 node ws-gateway.cjs",
    "test:e2e:mock": "npm run test:e2e:mock:start && npx cypress run --browser chrome; npm run test:e2e:mock:stop",
    "test:e2e:mock:start": "docker compose up -d --build && npx wait-on http://localhost:8089 http://localhost:8090 tcp:127.0.0.1:3001 tcp:127.0.0.1:3101 tcp:127.0.0.1:3102 --timeout 300000",
    "test:e2e:mock:stop": "docker compose down",
    "test:e2e": "npm run test:e2e:start && npx cypress run --browser chrome --config requestTimeout=300000,defaultCommandTimeout=300000,retries=1 --env USE_MOCK=false; npm run test:e2e:stop",
    "test:e2e:start": "docker compose up -d --build wheel_release_test_server wheel_release_test",
    "test:e2e:stop": "docker compose down",
    "test:e2e:remote": "npx cypress run --browser chrome --config baseUrl=$WHEEL_URL,requestTimeout=300000,defaultCommandTimeout=300000,retries=1 --env USE_MOCK=false",
    "test:e2e:gfarm": "npx dotenv -e cypress/e2e/gfarm/gfarm-e2e.env -- npx cypress run --browser chrome --spec 'cypress/e2e/gfarm/**/*.cy.js'",
    "test:e2e:gfarm:open": "npx dotenv -e cypress/e2e/gfarm/gfarm-e2e.env -- npx cypress open --spec 'cypress/e2e/gfarm/**/*.cy.js'",
    "start": "cd ../; npm start",
    "build": "cd ../; npm run build",
    "mocks": "mocks-server --config.fileSearchPlaces=mocks.config.cjs"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@cypress/skip-test": "^2.6.1",
    "@cypress/webpack-preprocessor": "^7.0.1",
    "@mocks-server/main": "^4.1.0",
    "@vitejs/plugin-vue": "^6.0.1",
    "@vue/compiler-sfc": "^3.5.18",
    "chrome": "^0.1.0",
    "cypress": "^15.5.0",
    "cypress-delete-downloads-folder": "^0.0.6",
    "cypress-real-events": "^1.15.0",
    "cypress-wait-until": "^3.0.2",
    "fs-extra": "^11.2.0",
    "graphlib": "^2.1.8",
    "http-proxy": "^1.18.1",
    "simple-ssh": "^1.1.1",
    "socket.io": "^4.8.1",
    "socketio-file-upload": "^0.7.3",
    "tar": "^7.4.3",
    "vite": "^7.1.12",
    "vue": "^3.5.22",
    "vuetify": "^3.10.8",
    "wait-on": "^9.0.4"
  }
}
